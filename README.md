# competitive-programming-haskell

Solved problems live in [`problems/`](problems/README.md), each as a standalone, runnable `.hs` file. `Main.hs` at the repo root is the scratch file used with VS Code's Code Runner while working on the current problem.
